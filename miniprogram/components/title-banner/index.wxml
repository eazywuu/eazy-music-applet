<!--components/title-banner/index.wxml-->
<view class="header">
    <view class="title">
    {{ title }}
    </view>
    <view class="more" wx:if="{{ !hideMore }}">
        {{ moreLabel }}
    </view>
</view>
