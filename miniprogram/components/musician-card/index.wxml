<!--components/musician-card/index.wxml-->
<view wx:if="{{ item }}" class="card" bindtap="onTab">
    <image class="photo" src="{{ item.photo }}" />
    <view class="name">{{ item.name }}</view>
    <view class="category">{{ item.category }}</view>
</view>
