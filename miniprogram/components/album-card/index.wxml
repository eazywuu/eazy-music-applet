<!--components/album-card/index.wxml-->
<view wx:if="{{ item }}" class="album-card" bindtap="onTab">
    <image class="image" src="{{ item.image }}" />
    <view class="title">{{ item.title }}</view>
    <view class="musician">{{ item.musician }}</view>
</view>
